<Window x:Class="Gym2.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gym2.Views"
        xmlns:vm="clr-namespace:Gym2.ViewModel"
        mc:Ignorable="d"
        Title="Inicio de sesión" Height="450" Width="800" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Background="#FF1E2A38">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="31*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Width="360" Height="360" Background="#FF2B3948" CornerRadius="12" HorizontalAlignment="Left" VerticalAlignment="Center" Padding="20" Margin="220,0,0,0">
            <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <TextBlock Text="INICIAR SESIÓN" Foreground="White" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,16"/>

                <TextBlock Text="Usuario" Foreground="#BFD1DCE6"/>
                <TextBox x:Name="txtUsuario" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,4,0,12" TextChanged="txtUsuario_TextChanged" />

                <TextBlock Text="Contraseña" Foreground="#BFD1DCE6"/>
                <TextBox x:Name="txtPassword" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,4,0,12" />

                <TextBlock Text="" Height="8" />

                <Button Content="INGRESAR" Command="{Binding LoginCommand}" Height="36" Background="DarkGray" Foreground="Gray" Margin="0,6,0,4" Click="Button_Click" />

                <Button Content="CANCELAR" Height="28" Background="DarkGray" Foreground="Gray" BorderThickness="0" Click="btnCancelar_Click" />

                <TextBlock HorizontalAlignment="Center" Margin="0,15,0,0" FontSize="12">
                    <Run Text="¿No tienes cuenta?" Foreground="White"/>
                    <Hyperlink Command="{Binding ShowRegisterCommand}" Foreground="SkyBlue" TextDecorations="Underline">
                        Regístrate aquí
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>

</Window>
